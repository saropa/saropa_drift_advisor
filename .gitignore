# Dart / Pub
.dart_tool/
.packages
build/
pubspec.lock

# IDE
.idea/
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
*.iml

# OS
.DS_Store
Thumbs.db

# Generated API docs (pub.dev generates its own)
doc/api/

# Reports (optional) — ignore generated reports but keep the organizer script
reports/*
!reports/organize_reports.py

# Secrets
.env

# Python (scripts)
__pycache__/
*.py[cod]
*.pyo
scripts/.publish_checkpoint.json

# Root workspace node_modules (husky)
node_modules/

# VS Code / Cursor extension (extension/)
extension/node_modules/
extension/out/
extension/*.vsix
extension/CHANGELOG.md
extension/ABOUT_SAROPA.md

# AI / LLM tooling config (Claude Code, Cursor, Copilot, etc.)
.claude/
CLAUDE.md
**/claude-code-*
.cursor/
.cursorignore
.cursorindexingignore
.cursorrules
.copilot/
.github/copilot-instructions.md
.aider*

# Root is the Dart package.
*.bak

# Saropa Log Capture (index & cache)
.saropa/

# Bug docs: keep report guide, ignore finish checklist
bugs/FINISH_GUIDE.md
# Internal /finish tooling defect record — narration-voice content stays out of
# the tracked package; the durable instruction lives in the global finish skill
# and the gitignored FINISH_GUIDE.md, not in the published repo.
bugs/BUG_finish_skill_narration_voice.md
!bugs/BUG_REPORT_GUIDE.md
