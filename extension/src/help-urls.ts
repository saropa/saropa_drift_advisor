/**
 * Stable public URLs for user help (connection, setup, troubleshooting).
 */

/** README section: VS Code / browser / auth / ports — opened from Schema Search troubleshooting. */
export const SAROPA_DRIFT_CONNECTION_DOC_URL =
  'https://github.com/saropa/saropa_drift_advisor#3-connect-a-client';
